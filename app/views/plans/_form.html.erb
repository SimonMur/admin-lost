<%= form_for(plan) do |f| %>
  <% if plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :cost %>
    <%= f.text_field :cost %>
  </div>
<div class="field">
    <%= f.label :number_of_posts_monthly %>
    <%= f.text_field :posts %>
  </div>
  <div class="field">
    <%= f.label :duration %>
    <%= f.text_field :duration %>
  </div>

  <div class="field">
    <%= f.label :support %>
    <%= f.check_box :support %>
  </div>

  <div class="field">
    <%= f.label :admin_panel %>
    <%= f.check_box :admin_panel %>
  </div>

  <div class="field">
    <%= f.label :lost_id_alerts %>
    <%= f.check_box :lost_id_alerts %>
  </div>

  <div class="field">
    <%= f.label :reported_id_alerts %>
    <%= f.check_box :reported_id_alerts %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
